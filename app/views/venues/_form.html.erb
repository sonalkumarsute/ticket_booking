<%= form_with(model: venue, local: true) do |form| %>
  <% if venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venue.errors.count, "error") %> prohibited this venue from being saved:</h2>

      <ul>
      <% venue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :venue_name, class: "form-control", required:true%>
  </div>

  <div class="field">
    <%= form.label :rows %>
    <%= form.number_field :rows, id: :venue_rows , class: "form-control", required:true%>
  </div>

  <div class="field">
    <%= form.label :cols %>
    <%= form.text_field :cols, id: :venue_cols, class: "form-control" , required:true%>
  </div>

  <div class="actions pull-right">
    <%= form.submit "Submit", class: "btn-sm btn-success" %>
  </div>
<% end %>
